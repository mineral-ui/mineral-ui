/* @flow */
export enum ALIGN {
  start = 'start',
  center = 'center',
  end = 'end',
  justify = 'justify'
};

export enum INTERNAL_TYPE {
  minimal = '_minimal',
  none = '',
  pills = '_pills',
  tabs = '_tabs'
};

export enum PREFIX {
  secondary = 'Secondary',
  primary = 'Primary'
};

export enum TYPE {
  pills = 'pills',
  tabs = 'tabs'
};
