/* @flow */
import { createStyledComponent } from '../../../../../library/styles';
import Flex from '../../../../../library/Flex';

export default createStyledComponent(Flex, () => ({
  margin: '-1em',
  minHeight: 200,
  position: 'relative'
}));
