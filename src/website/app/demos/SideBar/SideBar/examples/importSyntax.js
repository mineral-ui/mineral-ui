/* @flow */
export default {
  id: 'import-syntax',
  title: 'Import Syntax',
  description: `\`\`\`
import SideBar from 'mineral-ui/SideBar';
\`\`\`

<Callout title="Note">
  <p key={0}>
    SideBars exist in a layer above the app by default (see <a href="#props" key={0}>usePortal</a>). For illustration purposes the <a href="#props" key={0}>usePortal</a> prop was set to false in all of the following examples.
  </p>
</Callout>`
};
