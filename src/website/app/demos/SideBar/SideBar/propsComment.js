/* @flow */
import React from 'react';

export default (
  <div>
    <p>
      In addition to the props above, SideBar also accepts all props for{' '}
      <a href="/components/flex" key={0}>
        Flex
      </a>
      .
    </p>
    <p>
      <em key={1}>
        Undocumented properties will be applied to the root element.
      </em>
    </p>
  </div>
);
