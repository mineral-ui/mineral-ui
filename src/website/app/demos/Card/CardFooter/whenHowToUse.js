/* @flow */
export default `Use CardFooter to add a visually differentiated section to your
[Card](/components/card). It is best used for stateful information or
functionality, particularly when paired with a [\`variant\`](#variants).
CardFooter must always be the last thing in a Card.`;
