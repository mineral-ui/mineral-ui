/* @flow */
import React from 'react';

export default (
  <div>
    <p>
      In addition to the props above, StartEnd also accepts all props for{' '}
      <a href="/components/flex">Flex</a>, <strong>except</strong>{' '}
      <code>justifyContent</code> and <code>wrap</code>.
    </p>
    <p>
      <em>Undocumented properties will be applied to the root element.</em>
    </p>
  </div>
);
