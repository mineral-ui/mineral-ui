/* @flow */
import React from 'react';

export default (
  <div>
    <p>
      In addition to the props above, FlexItem also accepts all props for{' '}
      <a href="/components/box">Box</a> (and for{' '}
      <a href="/components/flex">Flex</a>, if <code>flex</code> is{' '}
      <code>true</code>).
    </p>
    <p>
      <em>Undocumented properties will be applied to the root element.</em>
    </p>
  </div>
);
