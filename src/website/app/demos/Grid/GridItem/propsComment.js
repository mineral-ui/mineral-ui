/* @flow */
import React from 'react';

export default (
  <div>
    <p>
      In addition to the props above, GridItem also accepts all props for{' '}
      <a href="/components/box">Box</a>, <strong>except</strong>{' '}
      <code>width</code>.
    </p>
    <p>
      <em>Undocumented properties will be applied to the root element.</em>
    </p>
  </div>
);
