/* @flow */
export default `Pagination offers a means to control the space consumed by a
large data set or other collection of items by limiting the page size and
providing navigation for access to all pages.`;
