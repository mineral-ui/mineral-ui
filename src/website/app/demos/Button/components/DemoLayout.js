/* @flow */
import { createStyledComponent } from '../../../../../styles';

export default createStyledComponent('div', {
  '& > button': {
    marginBottom: '0.5rem',
    marginRight: '0.5rem'
  }
});
