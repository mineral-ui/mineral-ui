/* @flow */
export default {
  id: 'import-syntax',
  title: 'Import Syntax',
  description: `\`\`\`
import Dialog from 'mineral-ui/Dialog';
\`\`\`

<Callout title="Note">
  <p key={0}>
    Dialogs normally exist in a layer above the app. All of the following
    examples except <a href="#trigger" key={0}>Trigger</a>, <a href="#modeless" key={1}>Modeless</a>, and <a href="#composition" key={3}>Composition</a> are
    static (they display inline, focus is never set nor trapped, and closing
    functions are disabled) for illustration purposes.
  </p>
</Callout>`
};
