/* @flow */
export default `MenuDividers separate [MenuItems](/components/menu-item) and
[MenuGroups](/components/menu-group) to establish hierarchy in a
[Menu](/components/menu) with several options.

MenuDividers are not merely design elements to visually separate options. Rather,
the are a way to logically group choices. Too many dividers will add unnecessary
weight to your Menu.`;
