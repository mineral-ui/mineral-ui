/* @flow */
export default `A MenuItem represents an option in a [Menu](/components/menu).
They can be used to trigger actions or navigate to a new location. MenuItems
provide context through optional variants, secondary text, or
[Icons](/components/icon).

A custom rendering hook is exposed to enable any extra functionality your app
requires.`;
