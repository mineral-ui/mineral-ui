/* @flow */
export default `CheckboxGroup is used to provide a simpler API for creating a
group of [Checkboxes](/components/checkbox).  It also provides proper row
spacing and an inline layout option.

Use CheckboxGroup to allow users to select multiple options from a list.

CheckboxGroups should be wrapped in a [FormField](/components/form-field) to
provide an accessible label and other features.  See the
[FormField example](#form-field) for details.`;
